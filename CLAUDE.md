# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

本仓库把所有共享的 agent 指令(开发命令、架构、约定)统一维护在 [AGENTS.md](AGENTS.md) 中。
下面通过 `@` 导入,Claude Code 会自动读取其完整内容,无需在两处重复维护。

@AGENTS.md
